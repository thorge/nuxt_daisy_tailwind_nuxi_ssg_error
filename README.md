# nuxt + daisyui / build errors

See [issue](https://github.com/saadeghi/daisyui/issues/3472)

## Install

Run `npx nuxi@latest upgrade --force`

## SSG (fails)

Run `npx nuxi generate` to show build errors.
